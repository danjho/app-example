name: app_example
description: "A new Flutter project."
publish_to: "none" #
version: 0.1.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=3.22.2"

dependencies:
  flutter:
    sdk: flutter
  lib_dependencies:
    git:
      url: https://github.com/danjho/lib-dependencies.git
  lib_core:
    git:
      url: https://github.com/danjho/lib-core.git
  micro_calculator:
    git:
      url: https://github.com/danjho/micro-calculator.git
  micro_home:
    git:
      url: https://github.com/danjho/micro-home.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
